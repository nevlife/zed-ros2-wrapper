# Parameter override applied by the multi camera launch files and zed2i_camera.py

/**:
    ros__parameters:
        general:
            pub_resolution: "NATIVE" # flux.raw_nv12 publishes the sensor-resolution ISP buffer
        flux:
            enable: true
            raw_nv12: false
            slot_count: 8
